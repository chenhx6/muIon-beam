# Project-owned figure checks

These small, selected checks were absorbed from the pinned Apache-2.0
`nature-figure` source at commit `287ee37542620711a56c7c58a73f44ef5c2bede0`.
The project workflow invokes these copies; the vendored upstream directory is not
modified during ordinary project work. Keep the source commit and copied-file
hashes in review notes when updating these checks.
