# Task routing

Route geometry legality and layout to 3D, continuous fields and trajectories to
COMSOL, and particle transport/statistics to Geant4. A coupled question is
serially routed 3D -> COMSOL -> Geant4 in the first version. Direct module
entry points still write `research-state` and never bypass contract or issue
report requirements when a full contract is available.
