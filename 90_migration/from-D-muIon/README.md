# 旧工作区迁移占位

`D:\muIon` 当前正在执行任务，本阶段不迁移、不复制、不整理、不删除其中内容。

未来迁移必须单独建立任务卡，并按以下顺序进行：

```text
source-index
  → selected-tasks
  → imported-runs
  → migration-notes
  → formalize
```

迁移时保留原始路径、原始时间、来源文件、已知版本和缺失字段。不能根据文件名猜测历史参数。
