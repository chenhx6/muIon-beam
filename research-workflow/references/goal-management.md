# Goal management

Keep `parent_goal`, `current_question`, `current_task` and the selected module
consistent. A local execution result never changes the research objective by
itself. A changed objective creates a new goal or task and records the decision.
